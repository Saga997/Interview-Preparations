package com.collection.illustration.ArrayQuestions;

public class MoveZeroesToEndOfArray {
    //Brute force would be int[] arr ={1,2,4,5,0,5,9}
    //traverse the array and put all the non zero elements in one temp array
    //then in the original array from the start index put those elements and the remaining keep them as zero
    //
}
